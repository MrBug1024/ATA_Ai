services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://10.0.10.2:8081}
    restart: unless-stopped
    # ports managed by Coolify
    # ports:
    #   - "3000:3000"
